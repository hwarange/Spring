<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
   ============================================================
    BoardMapper.xml : 실제 SQL 쿼리문 + 결과 매핑

    * namespace = BoardDao 인터페이스의 FQCN (반드시 풀패키지명)
    * SELECT / INSERT / UPDATE / DELETE 구문의 id 속성은
       DAO 메서드 이름과 정확히 일치해야 함
    * #{...} 문법은 내부적으로 PreparedStatement 의 ? 자리로 치환됨
   ============================================================
-->
<mapper namespace="com.ssafy.board.model.dao.BoardDao">

	<!-- TODO 1-02A: resultMap 정의
	                 - id 는 "boardMap"
	                 - type 은 Board (typeAlias 별칭 활용)
	                 - snake_case 컬럼(reg_date, view_cnt)을 camelCase 필드로 매핑 -->
	                 
	<resultMap type="Board" id="BoardMap">
		<result column="reg_date" property="regDate" />
		<result column="view_cnt" property="viewCnt" />
	</resultMap>


	<!-- TODO 1-02B: 전체 게시글 조회 — SELECT 구문
	                 - id 는 selectAll
	                 - resultType 으로 Board 사용
	                 - 컬럼 매핑은 SELECT 절의 별칭(AS)으로 처리 -->
	
	<select id="selectAll" resultType="Board">
		SELECT id, title, writer, content, reg_date AS regDate, view_cnt AS viewCnt
		FROM board
	</select>
	
<!-- 	<select id="selectAll2" resultMap="BoardMap">
		SELECT *
		FROM board
	</select> -->
	

	<!-- TODO 1-02C: 단건 조회 — SELECT 구문
	                 - id 는 selectOne
	                 - parameterType 은 int
	                 - resultMap 으로 위에서 정의한 boardMap 활용
	                   -> SELECT * 만 써도 됨 -->
	                   
	<select id="selectOne" parameterType="int" resultMap="BoardMap">
		SELECT *
		FROM board
		WHERE id = #{id}
	</select>


	<!-- TODO 1-02D: 게시글 등록 — INSERT 구문
	                 - id 는 insertBoard
	                 - parameterType 은 Board
	                 - title / writer / content 만 INSERT
	                   (id, reg_date, view_cnt 는 DB 자동)
	                 - #{필드명} 으로 객체의 getter 호출 -->
	<insert id="insertBoard" parameterType="Board">
			INSERT INTO board(title, writer, content)
			VALUES (#{title}, #{writer}, #{content})
	</insert>

	<!-- TODO 1-02E: 게시글 수정 — UPDATE 구문
	                 - id 는 updateBoard
	                 - parameterType 은 Board
	                 - SET 절은 title, content 만 (writer 는 수정 X)
	                 - WHERE 는 id -->
	<update id="updateBoard" parameterType="Board">
		UPDATE board
		SET title = #{title}, content = #{content}
		WHERE id = #{id}
	</update>

	<!-- TODO 1-02F: 게시글 삭제 — DELETE 구문
	                 - id 는 deleteBoard
	                 - parameterType 은 int
	                 - WHERE 는 id -->
	<delete id="deleteBoard">
		DELETE FROM Board
		WHERE id = #{id}
	</delete>

	<!-- TODO 1-02G: 조회수 1 증가 — UPDATE 구문
	                 - id 는 updateViewCnt
	                 - parameterType 은 int
	                 - view_cnt 자기 자신 + 1 패턴
	                   (자바에서 select->+1->update 노가다 X) -->
	<update id="updateViewCnt" parameterType="int">
		UPDATE board
		SET view_cnt = view_cnt + 1
		WHERE id = #{id}
	</update>

</mapper>
